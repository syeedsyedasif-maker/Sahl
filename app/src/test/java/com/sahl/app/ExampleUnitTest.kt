package com.sahl.app

import org.junit.Assert.assertEquals
import org.junit.Test

/** Local unit test, runs on the development JVM. */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
